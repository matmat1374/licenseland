# LOOP.md — حلقهٔ بهبود مستمر لیسنو

**هدف:** `liceno.ir` — مخزن: `C:\Users\matin\Documents\antigravity\licenseland`
**مبنا:** [loop-engineering](https://github.com/cobusgreyling/loop-engineering) — الگوهای `daily-triage` + مسیر `refactor/change`

---

## ۱. هدف

رساندن سایت به بهترین سطح ممکن در **همهٔ** محورها به‌طور هم‌زمان:
جریان خرید و تحویل (مشتری **و** ادمین)، تأمین از API تأمین‌کننده، کیفیت تعریف محصول،
قیمت‌گذاری، سئو، UI/UX، سرعت، بازبینی کد، دیزاین، مارکتینگ، گیمیفیکیشن،
دسته‌بندی/عنوان/تگ محصولات، همگام‌سازی کاتالوگ، مانیتورینگ و امنیت — با شاهد قابل‌بررسی در هر تکرار.

## ۲. حلقه (۵ تکرار متوالی، سپس شروع دوباره)

هر تکرار دقیقاً همین چرخه را طی می‌کند:

| فاز | خروجی | شرط عبور |
|---|---|---|
| ① کشف | `docs/loop/backlog-<n>.md` — یافته‌های اندازه‌گیری‌شده به تفکیک محور | هر یافته **شاهد** دارد (URL، کوئری، فایل:خط) |
| ② اولویت‌بندی | ۱۰ مورد برتر با امتیاز (اثر × اطمینان ÷ زحمت) | هیچ موردی بدون محور و شاهد نیست |
| ③ اصلاح | حداکثر ۵ اصلاح در هر تکرار | یک اصلاح = یک موضوع؛ بدون بازنویسی نامرتبط |
| ④ راستی‌آزمایی | `docs/loop/verify-<n>.md` — اجرای واقعی | نقش **راستی‌آزما** مستقل از اصلاح‌کننده |
| ⑤ ثبت | یک ردیف در `loop-run-log.md` | قالب اجباری زیر |

پس از تکرار پنجم: اندازه‌گیری کلی دوباره، و حلقه از ① ادامه می‌یابد مادامی که امتیاز بالا می‌رود.

## ۳. محورهای بررسی (بیست‌گانه)

| # | محور | ابزار سنجش |
|---|---|---|
| ۱ | جریان خرید→تحویل (مشتری) | تست سرتاسری، وضعیت‌ها، ایمیل‌ها |
| ۲ | جریان عملیات (ادمین) | مسیرهای `/api/admin/*`، پنل عملیات |
| ۳ | تأمین از تأمین‌کننده | `SupplierOrder`/`SupplierLog`، هویت محصول |
| ۴ | کیفیت تعریف محصول | ممیزی کاتالوگ، تکراری‌ها |
| ۵ | عنوان/تگ/دسته‌بندی | استاندارد نام‌گذاری |
| ۶ | قیمت‌گذاری | هم‌خوانی هزینه/فروش، حاشیه |
| ۷ | سئو | site­map، canonical، JSON-LD، ایندکس |
| ۸ | سرعت | وزن صفحه، تعداد درخواست، تصاویر، فونت |
| ۹ | UI | یکدستی بصری، واکنش‌گرایی |
| ۱۰ | UX | مسیر کاربر، حالت خالی/خطا |
| ۱۱ | دسترس‌پذیری | کنتراست، برچسب، کیبورد |
| ۱۲ | بازبینی کد | بدهی، تکرار، تایپ |
| ۱۳ | امنیت | هدرها، مجوزدهی، رازها، ورودی |
| ۱۴ | تست | پوشش kernel + سرتاسری |
| ۱۵ | مانیتورینگ و گزارش | لاگ، هشدار، متریک |
| ۱۶ | ایمیل تراکنشی | صف، retry، لاگ، SPF/DKIM/DMARC |
| ۱۷ | مارکتینگ | جذب، UTM، نرخ تبدیل |
| ۱۸ | گیمیفیکیشن/وفاداری | امتیاز، بج، پله |
| ۱۹ | همگام‌سازی کاتالوگ | تطبیق API، idempotency |
| ۲۰ | عملیات و تحویل تیم | SOP، نقش‌ها، رانبوک |

## ۴. قیدها (الزامی)

- هرگز `.env`، رازها و اعتبارنامه‌ها تغییر نکنند.
- دیپلوی پروداکشن فقط با **شاهد** سبز بودن بیلد و پاسخ‌گویی URL عمومی.
- هرگز تست برای سبز شدن غیرفعال نشود.
- یک اصلاح، اصلاح دیگر را با خود نیاورد: ۱ مورد = ۱ تغییر.
- هر نوشتن روی دیتابیس پروداکشن: اول بکاپ، بعد `--dry`، بعد `--apply`.
- حداکثر ۳ تلاش برای هر مورد؛ بعد از آن به backlog منتقل و سراغ مورد بعدی.
- مسیر بازگردانی پیش از هر اقدام مخرب مستند شود.

## ۵. بودجه

| حلقه | اجرا/روز | توکن/روز | حداکثر اصلاح در هر اجرا |
|---|---|---|---|
| لیسنو full-site | ۳ | ۴۰۰k | ۵ |

عبور از بودجه → توقف، ثبت رخداد در `loop-run-log.md`، یادداشت در `STATE.md`.

## ۶. دروازهٔ راستی‌آزمایی (اجباری)

هیچ اصلاحی بدون **هر سه** پذیرفته نمی‌شود:
1. **شاهد محلی** — بیلد/تایپ‌چک/تست اجرا شده و خروجی نقل شده است.
2. **شاهد بیرونی** — URL عمومی یا کوئری‌ای که تغییر را نشان دهد.
3. **ضد‌رگرسیون** — آنچه قبلاً کار می‌کرد هنوز کار می‌کند.

نقش راستی‌آزما **مستقل** از اصلاح‌کننده است و تلاش می‌کند رد کند، نه تأیید.

## ۶.۵ دروازه‌های انسانی (Human gates)

طبق `docs/safety.md`:

| اقدام | نیاز به تأیید انسانی |
|---|---|
| گزارش‌گیری و اندازه‌گیری | ❌ لازم نیست |
| اصلاح کد + تست | ❌ لازم نیست (L2) |
| دیپلوی پروداکشن | ✅ **لازم است** |
| تغییر اسکیما/دادهٔ زنده | ✅ **لازم است** (با بکاپ) |
| تغییر قیمت، سیاست بازپرداخت، تنظیمات مالی | ✅ **لازم است** |
| ادغام روی `main` | ✅ **لازم است** |

مسیرهای ممنوع: `.env`، رازها، کلیدها، پیکربندی زیرساخت — بدون استثنا.

## ۷. قالب ثبت

```json
{ "run_id":"", "iteration":1, "axes":[], "items_found":0, "fixes":0,
  "verifier":"pass|reject", "evidence":[], "tokens_estimate":0, "outcome":"" }
```

## ۸. کلید توقف

- فایل نشانه: `docs/loop/PAUSED` → هر اجرا فوراً متوقف شود.
- برچسب/کلیدواژه: `loop-pause-all`.
- از سرگیری فقط پس از حذف نشانه.

## ۹. وضعیت جاری

| تکرار | تاریخ | یافته | اصلاح | راستی‌آزما |
|---|---|---|---|---|
| ۱ | — | — | — | — |

<!-- حلقه از این‌جا می‌نویسد -->
