# Backlog — تکرار ۱ (کشف)

**تاریخ:** ۲۴ شهریور ۱۴۰۵ · **روش:** اندازه‌گیری زندهٔ liceno.ir + بازبینی مخزن

## یافته‌ها (هر مورد با شاهد)

| # | محور | یافته | شاهد | اثر | زحمت |
|---|---|---|---|---|---|
| ۱ | ۱۳ امنیت | CSP اجازهٔ `unsafe-inline` و `unsafe-eval` برای اسکریپت می‌دهد | هدر `content-security-policy` پاسخ `/` | بالا | متوسط |
| ۲ | ۸ سرعت | HTML صفحهٔ اصلی ۳۲۵KB با ۳۳ تگ اسکریپت | `GET /` → ۳۲۵KB | بالا | متوسط |
| ۳ | ۱۲ بازبینی کد | `npm run typecheck` فقط `kernel/**` را چک می‌کند؛ `src/` پوشش ندارد | `tsconfig.domain.json include` = kernel only | بالا | کم |
| ۴ | ۱۹ کاتالوگ | ۷۱۵ آدرس در سایت‌مپ اما ۶۵۶ آیتم در فید ترب → ۵۹ محصول ناهمخوان | `/sitemap.xml` vs `/api/torob` | متوسط | کم |
| ۵ | ۳ تأمین | نسخهٔ منتشرشدهٔ `supplier.ts` بلوک معیوب بازنویسی `const` دارد | خطای کامپایل لوکال در `supplier.ts:1520` | بالا | کم |
| ۶ | ۱۶ ایمیل | زیرساخت ایمیل ساخته شد اما هیچ ارائه‌دهنده‌ای پیکربندی نشده (`SKIPPED`) | `activeProvider()` → none | بالا | کم (نیاز به اعتبارنامه) |
| ۷ | ۲۰ عملیات | SOP/صفحه‌گسترده هنوز نوشته نشده | — | متوسط | متوسط |

## وضعیت سنجه‌ها در شروع

- سئو: title ✅ · canonical ✅ · JSON-LD ×۴ ✅ · متاتگ محصول ۴/۴ ✅
- دسترس‌پذیری: lang/dir ✅ · یک h1 ✅ · تصویر بدون alt: ۰ ✅
- امنیت: HSTS ✅ · X-Frame-Options DENY ✅ · nosniff ✅ · Referrer-Policy ✅
- کاتالوگ: سایت‌مپ ۷۱۵ · فید ۶۵۶

## اولویت‌بندی (۱۰ مورد برتر)

۱. رفع باگ `supplier.ts` و انتشار مجدد (ریسک worker سینک)
۲. پوشش تایپ‌چک برای `src/` در CI
۳. سخت‌سازی CSP
۴. کاهش وزن HTML صفحهٔ اصلی
۵. هم‌راستاسازی فید ترب با سایت‌مپ
۶. پیکربندی ارائه‌دهندهٔ ایمیل + SPF/DKIM/DMARC
۷. SOP + صفحه‌گستردهٔ پیگیری
۸. مانیتورینگ: هشدار صف ایمیل و خطای سینک
۹. بازبینی قیمت‌گذاری و حاشیه
۱۰. ممیزی دسترس‌پذیری کامل (WCAG)
