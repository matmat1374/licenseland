import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { await prisma.category.updateMany({ where: { slug: 'ai' }, data: { sortOrder: 1 } }); await prisma.category.updateMany({ where: { slug: 'design' }, data: { sortOrder: 2 } }); await prisma.category.updateMany({ where: { slug: 'software' }, data: { sortOrder: 3 } }); await prisma.category.updateMany({ where: { slug: 'streaming' }, data: { sortOrder: 4 } }); console.log('Updated categories'); } main().finally(() => prisma.$disconnect());
