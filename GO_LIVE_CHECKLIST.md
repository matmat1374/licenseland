# GO-LIVE CHECKLIST — LICENSELAND

هر مورد باید پیش از پذیرش اولین ریال پول واقعی علامت بخورد. موارد مشخص‌شده با ⛔ «توقف‌دهنده» هستند.

## امنیت — پیش‌نیاز مطلق

- [ ] ⛔ توکن GitHub ارسال‌شده در چت ابطال شده است.
- [ ] ⛔ `.env` و `db/custom.db` از تاریخچه گیت purge و به `.gitignore` اضافه شده‌اند.
- [ ] ⛔ همه کلیدهای موجود در `.env` قدیمی باطل و دوباره صادر شده‌اند.
- [ ] ⛔ `LICENSE_VAULT_KEY` (۳۲ بایت) تولید، در مدیریت راز ذخیره، و پشتیبان جداگانه گرفته شده.
- [ ] ⛔ تمام فراخوانی‌های تامین‌کننده سرورساید؛ جستجوی `anb_` در باندل کلاینت بدون نتیجه.
- [ ] 2FA (TOTP) برای همه حساب‌های ادمین اجباری شده.
- [ ] Rate Limit و Lockout روی ورود، فراموشی رمز و پرداخت فعال است.
- [ ] هدرهای امنیتی (`docs/SECURITY.md` ۵.۳) در پاسخ واقعی دیده می‌شوند.

## پول و تحویل

- [ ] ⛔ درگاه ایرانی با هویت حقوقی معتبر و `MERCHANT_ID` واقعی متصل شده.
- [ ] ⛔ مسیر Verify سرورساید تست شده؛ Callback تکراری دوبار شارژ نمی‌کند.
- [ ] ⛔ موجودی پیش‌پرداخت USD تامین‌کننده شارژ شده و هشدار کف فعال است.
- [ ] ⛔ نرخ دلار با منبع و حداکثر سن تعیین شده؛ نرخ کهنه فروش را متوقف می‌کند.
- [ ] یک سفارش واقعی کم‌مبلغ انتها-به-انتها انجام شده (پرداخت → خرید → تحویل → ایمیل → فاکتور).
- [ ] سناریوی شکست تامین‌کننده تست شده: ریفاند خودکار به کیف پول + تیکت خودکار دیده شده.
- [ ] اسکریپت تطبیق روزانه درگاه اجرا می‌شود و خروجی صفر-مغایرت دارد.
- [ ] مانده کیف پول در UI با جمع دفتر دوطرفه مطابقت دارد.

## کیفیت فنی

- [ ] `npm run verify` (typecheck + lint + test + build) سبز است.
- [ ] CI روی PR اجباری شده و merge بدون CI سبز ممنوع است.
- [ ] `npm audit` بدون CVE سطح High/Critical.
- [ ] Playwright: مسیر موفق + پرداخت ناموفق + عدم موجودی + خطای تامین‌کننده + Callback تکراری.
- [ ] Lighthouse موبایل هر چهار شاخص ≥ ۹۵ روی صفحه محصول واقعی.
- [ ] axe بدون تخلف Serious/Critical.
- [ ] متن فارسی با فونت Vazirmatn در RTL بدون شکست چیدمان.

## سئو و محتوا

- [ ] ⛔ `AggregateRating` فقط از نظرات واقعی تاییدشده تولید می‌شود؛ پیش‌فرض ۵.۰ حذف شده است.
- [ ] `Review.approved` دیگر پیش‌فرض `true` نیست.
- [ ] `sitemap.xml` داینامیک، `robots.txt`، canonical و breadcrumb فعالند.
- [ ] محصولات بدون موجودی یا بدون توضیح یکتا `noindex` می‌خورند.
- [ ] Search Console تایید مالکیت و sitemap ثبت شده.

## حقوقی و انطباق (مشاوره حقوقی قطعی نیست)

- [ ] ⛔ مجوز استفاده از درگاه پرداخت با ماهیت واقعی کسب‌وکار مطابقت دارد.
- [ ] شرایط وندور محصولات (بازفروشی لایسنس/اشتراک) بررسی شده — ریسک مسدودی حساب کاربر نهایی بر عهده کیست؟
- [ ] سیاست بازگشت وجه، شرایط استفاده و حریم خصوصی منتشر شده‌اند.
- [ ] تکلیف مالیاتی فروش دیجیتال و صدور فاکتور با حسابدار روشن شده.

## عملیات

- [ ] پشتیبان روزانه فعال + یک بازیابی موفق تمرین شده.
- [ ] `/healthz` و `/readyz` پاسخ می‌دهند و پایش متصل است.
- [ ] هشدار خطای تحویل و هشدار کف موجودی USD به تلگرام/ایمیل متصل است.
- [ ] صف Dead-Letter قابل مشاهده و Replay از پنل است.
